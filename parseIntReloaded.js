function parseInt(string) {
    // Lex: tokenize a stream of text
    //   chop into discrete tokens
    let tokens = string.split(" ")
    //   identify token type (not)
    //   output a stream of tokens
}