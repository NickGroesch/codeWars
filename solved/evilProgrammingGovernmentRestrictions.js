var anarchy = function () {
    const one = true
    const two = one + one
    const three = two + one
    let str = `I can write numbers like, ${one.toString()}, ${two.toString()}, ${three.toString()}.`;
    return str
}
console.log(anarchy())
console.log(true + true)